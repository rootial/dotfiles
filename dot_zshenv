# Shell Environment (ALL shells: interactive, non-interactive, login, scripts)
# Keep minimal — only truly universal variables. No PATH, no output.

export EDITOR="vim"
export LANG="en_US.UTF-8"
export COLORTERM="truecolor"
